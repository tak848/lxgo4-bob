// Code generated by BobGen psql v0.42.1-0.20260305085859-d9c40b0abedd. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package queries

import (
	"context"
	"fmt"
	"strings"
	"testing"

	"github.com/google/go-cmp/cmp"
	"github.com/google/uuid"
	"github.com/stephenafamo/bob"
	"github.com/stephenafamo/bob/dialect/psql"
	testutils "github.com/stephenafamo/bob/test/utils"
)

var beforeTestingGetProjectTaskStats = func(context.Context, bob.Tx) (uuid.UUID, error) {
	return random_uuid_UUID(nil), nil
}

func TestGetProjectTaskStats(t *testing.T) {
	t.Run("Base", func(t *testing.T) {
		var sb strings.Builder

		query := GetProjectTaskStats(random_uuid_UUID(nil))

		if _, err := query.WriteQuery(t.Context(), &sb, 1); err != nil {
			t.Fatal(err)
		}

		if diff := cmp.Diff(getProjectTaskStatsSQL, sb.String()); diff != "" {
			t.Fatalf("unexpected result (-got +want):\n%s", diff)
		}
	})

	t.Run("Mod", func(t *testing.T) {
		var sb strings.Builder

		query := GetProjectTaskStats(random_uuid_UUID(nil))

		if _, err := psql.Select(query).WriteQuery(t.Context(), &sb, 1); err != nil {
			t.Fatal(err)
		}

		queryDiff, err := testutils.QueryDiff(getProjectTaskStatsSQL, sb.String(), formatQuery)
		if err != nil {
			t.Fatal(err)
		}
		if queryDiff != "" {
			fmt.Println(sb.String())
			t.Fatalf("unexpected result (-got +want):\n%s", queryDiff)
		}
	})

	t.Run("Scanning", func(t *testing.T) {
		if testDB == nil {
			t.Skip("skipping test, no DSN provided")
		}

		ctxTx, cancel := context.WithCancel(t.Context())
		defer cancel()

		tx, err := testDB.Begin(ctxTx)
		if err != nil {
			t.Fatalf("Error starting transaction: %v", err)
		}

		defer func() {
			if err := tx.Rollback(ctxTx); err != nil {
				t.Fatalf("Error rolling back transaction: %v", err)
			}
		}()

		arg0, beforeHookErr := beforeTestingGetProjectTaskStats(ctxTx, tx)
		if beforeHookErr != nil {
			t.Fatalf("Error in beforeTesting: %v", beforeHookErr)
		}

		query, args, err := bob.Build(ctxTx, psql.Select(GetProjectTaskStats(arg0)))
		if err != nil {
			t.Fatal(err)
		}

		rows, err := tx.QueryContext(ctxTx, query, args...)
		if err != nil {
			t.Fatal(err)
		}
		defer func() {
			if err := rows.Close(); err != nil {
				t.Fatalf("Error closing rows: %v", err)
			}
		}()

		columns, err := rows.Columns()
		if err != nil {
			t.Fatal(err)
		}

		if len(columns) != 5 {
			t.Fatalf("expected %d columns, got %d", 5, len(columns))
		}

		if columns[0] != "id" {
			t.Fatalf("expected column %d to be %s, got %s", 0, "id", columns[0])
		}

		if columns[1] != "name" {
			t.Fatalf("expected column %d to be %s, got %s", 1, "name", columns[1])
		}

		if columns[2] != "total_tasks" {
			t.Fatalf("expected column %d to be %s, got %s", 2, "total_tasks", columns[2])
		}

		if columns[3] != "done_tasks" {
			t.Fatalf("expected column %d to be %s, got %s", 3, "done_tasks", columns[3])
		}

		if columns[4] != "active_tasks" {
			t.Fatalf("expected column %d to be %s, got %s", 4, "active_tasks", columns[4])
		}
	})
}

var beforeTestingGetMemberTaskSummary = func(context.Context, bob.Tx) (uuid.UUID, error) {
	return random_uuid_UUID(nil), nil
}

func TestGetMemberTaskSummary(t *testing.T) {
	t.Run("Base", func(t *testing.T) {
		var sb strings.Builder

		query := GetMemberTaskSummary(random_uuid_UUID(nil))

		if _, err := query.WriteQuery(t.Context(), &sb, 1); err != nil {
			t.Fatal(err)
		}

		if diff := cmp.Diff(getMemberTaskSummarySQL, sb.String()); diff != "" {
			t.Fatalf("unexpected result (-got +want):\n%s", diff)
		}
	})

	t.Run("Mod", func(t *testing.T) {
		var sb strings.Builder

		query := GetMemberTaskSummary(random_uuid_UUID(nil))

		if _, err := psql.Select(query).WriteQuery(t.Context(), &sb, 1); err != nil {
			t.Fatal(err)
		}

		queryDiff, err := testutils.QueryDiff(getMemberTaskSummarySQL, sb.String(), formatQuery)
		if err != nil {
			t.Fatal(err)
		}
		if queryDiff != "" {
			fmt.Println(sb.String())
			t.Fatalf("unexpected result (-got +want):\n%s", queryDiff)
		}
	})

	t.Run("Scanning", func(t *testing.T) {
		if testDB == nil {
			t.Skip("skipping test, no DSN provided")
		}

		ctxTx, cancel := context.WithCancel(t.Context())
		defer cancel()

		tx, err := testDB.Begin(ctxTx)
		if err != nil {
			t.Fatalf("Error starting transaction: %v", err)
		}

		defer func() {
			if err := tx.Rollback(ctxTx); err != nil {
				t.Fatalf("Error rolling back transaction: %v", err)
			}
		}()

		arg0, beforeHookErr := beforeTestingGetMemberTaskSummary(ctxTx, tx)
		if beforeHookErr != nil {
			t.Fatalf("Error in beforeTesting: %v", beforeHookErr)
		}

		query, args, err := bob.Build(ctxTx, psql.Select(GetMemberTaskSummary(arg0)))
		if err != nil {
			t.Fatal(err)
		}

		rows, err := tx.QueryContext(ctxTx, query, args...)
		if err != nil {
			t.Fatal(err)
		}
		defer func() {
			if err := rows.Close(); err != nil {
				t.Fatalf("Error closing rows: %v", err)
			}
		}()

		columns, err := rows.Columns()
		if err != nil {
			t.Fatal(err)
		}

		if len(columns) != 5 {
			t.Fatalf("expected %d columns, got %d", 5, len(columns))
		}

		if columns[0] != "id" {
			t.Fatalf("expected column %d to be %s, got %s", 0, "id", columns[0])
		}

		if columns[1] != "name" {
			t.Fatalf("expected column %d to be %s, got %s", 1, "name", columns[1])
		}

		if columns[2] != "assigned_tasks" {
			t.Fatalf("expected column %d to be %s, got %s", 2, "assigned_tasks", columns[2])
		}

		if columns[3] != "completed_tasks" {
			t.Fatalf("expected column %d to be %s, got %s", 3, "completed_tasks", columns[3])
		}

		if columns[4] != "overdue_tasks" {
			t.Fatalf("expected column %d to be %s, got %s", 4, "overdue_tasks", columns[4])
		}
	})
}

var beforeTestingGetWorkspaceDashboard = func(context.Context, bob.Tx) (uuid.UUID, error) {
	return random_uuid_UUID(nil), nil
}

func TestGetWorkspaceDashboard(t *testing.T) {
	t.Run("Base", func(t *testing.T) {
		var sb strings.Builder

		query := GetWorkspaceDashboard(random_uuid_UUID(nil))

		if _, err := query.WriteQuery(t.Context(), &sb, 1); err != nil {
			t.Fatal(err)
		}

		if diff := cmp.Diff(getWorkspaceDashboardSQL, sb.String()); diff != "" {
			t.Fatalf("unexpected result (-got +want):\n%s", diff)
		}
	})

	t.Run("Mod", func(t *testing.T) {
		var sb strings.Builder

		query := GetWorkspaceDashboard(random_uuid_UUID(nil))

		if _, err := psql.Select(query).WriteQuery(t.Context(), &sb, 1); err != nil {
			t.Fatal(err)
		}

		queryDiff, err := testutils.QueryDiff(getWorkspaceDashboardSQL, sb.String(), formatQuery)
		if err != nil {
			t.Fatal(err)
		}
		if queryDiff != "" {
			fmt.Println(sb.String())
			t.Fatalf("unexpected result (-got +want):\n%s", queryDiff)
		}
	})

	t.Run("Scanning", func(t *testing.T) {
		if testDB == nil {
			t.Skip("skipping test, no DSN provided")
		}

		ctxTx, cancel := context.WithCancel(t.Context())
		defer cancel()

		tx, err := testDB.Begin(ctxTx)
		if err != nil {
			t.Fatalf("Error starting transaction: %v", err)
		}

		defer func() {
			if err := tx.Rollback(ctxTx); err != nil {
				t.Fatalf("Error rolling back transaction: %v", err)
			}
		}()

		arg0, beforeHookErr := beforeTestingGetWorkspaceDashboard(ctxTx, tx)
		if beforeHookErr != nil {
			t.Fatalf("Error in beforeTesting: %v", beforeHookErr)
		}

		query, args, err := bob.Build(ctxTx, psql.Select(GetWorkspaceDashboard(arg0)))
		if err != nil {
			t.Fatal(err)
		}

		rows, err := tx.QueryContext(ctxTx, query, args...)
		if err != nil {
			t.Fatal(err)
		}
		defer func() {
			if err := rows.Close(); err != nil {
				t.Fatalf("Error closing rows: %v", err)
			}
		}()

		columns, err := rows.Columns()
		if err != nil {
			t.Fatal(err)
		}

		if len(columns) != 5 {
			t.Fatalf("expected %d columns, got %d", 5, len(columns))
		}

		if columns[0] != "project_count" {
			t.Fatalf("expected column %d to be %s, got %s", 0, "project_count", columns[0])
		}

		if columns[1] != "member_count" {
			t.Fatalf("expected column %d to be %s, got %s", 1, "member_count", columns[1])
		}

		if columns[2] != "total_tasks" {
			t.Fatalf("expected column %d to be %s, got %s", 2, "total_tasks", columns[2])
		}

		if columns[3] != "done_tasks" {
			t.Fatalf("expected column %d to be %s, got %s", 3, "done_tasks", columns[3])
		}

		if columns[4] != "overdue_tasks" {
			t.Fatalf("expected column %d to be %s, got %s", 4, "overdue_tasks", columns[4])
		}
	})
}
